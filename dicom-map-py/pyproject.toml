[build-system]
requires = ["maturin>=1.5,<2.0"]
build-backend = "maturin"

[project]
name = "dicom-map"
version = "0.2.7"
description = "Memory-mapped O(log n) DICOM tag dictionary (private + public tags)"
readme = "../README.md"
license = { text = "Apache-2.0" }
requires-python = ">=3.8"
classifiers = [
    "Programming Language :: Rust",
    "Programming Language :: Python :: 3",
    "Topic :: Scientific/Engineering :: Medical Science Apps.",
]

[tool.maturin]
features = ["pyo3/extension-module"]
module-name = "dicom_map._dicom_map"
manifest-path = "Cargo.toml"
python-source = "python"
# Bundle LICENSE into the sdist (the license lives at the repo root and
# maturin include forbids `..`, so a copy is co-located here).
include = [{ path = "LICENSE", format = "sdist" }]
